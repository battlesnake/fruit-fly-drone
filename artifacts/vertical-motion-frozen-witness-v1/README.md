# Vertical-motion frozen-witness finite-step audit v1

The training-only causal audit completed and its independent terminal checker
passed. The exact frozen direction improved the aggregate proposal-25 objective
and every protected direction stratum at all four tested step lengths. The two
largest lengths met the preregistered effect-size gates; the smaller two produced
the same improvement signs but were below the minimum effect sizes.

This demonstrates useful finite-step common-descent leverage inside the existing
24 type-shared T4/T5 parameters. It rules out both “larger batch alone” and an
immediate anatomy expansion as the next justified step: ordinary Adam had the
wrong tradeoff, while a constrained direction in the same parameter space works.

The compact [`report.json`](report.json) records the witness qualification,
four-trial decision data, terminal controls, and hashes of the complete ignored
report. Parameters, the untouched Adam state, and source connectome were restored
exactly. Development and acceptance data remained sealed and no candidate was
retained.

This result authorizes only separately preregistering common-descent or constrained
training in the same 24 parameters. It does not commission vertical direction
selectivity or authorize motion routing, hover, gate flight, or promotion.
