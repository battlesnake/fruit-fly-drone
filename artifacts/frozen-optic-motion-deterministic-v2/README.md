# Deterministic frozen T4/T5 optic-motion confirmation v2

This compact artifact records the deterministic confirmation preregistered in `800c762` and
implemented in `def023d`. The full local report is
`runs/optic-motion/frozen-t4t5-audit-002/report.json`; its SHA-256 is
`11305380a565651c25f7d0b8e0a564b42bddc1e457b7e7b575fb3839583e5af8`.

All three fresh replay processes and the distinct main process produced the same complete CPU
response-tree hash, `a1180375cb7825639c86d8793e510a40dc03972f13b60b44c9af374932d36204`.
Their runtime and stable metadata hashes also matched, and their exclusive claim/terminal proofs
validated. K32 versus K64 passed every solver limit, all states and outputs were finite, common
terminal inputs were exact, and the source was restored exactly.

The unchanged scientific rules therefore validly classify the frozen source as
`frozen_optic_motion_module_validly_uncommissioned`. The motion-window T4/T5 sign and DSI gates
failed, and stationary anatomical-opponent RMS was 6.12 times moving RMS. This pattern is not a
simple reversed direction code: T4 c-d was usually positive for both directions, while T5 c-d
was usually negative for both directions. Literal reversal passed overall, but its ON/OFF pathway
swap makes that much weaker evidence than balanced within-pathway direction selectivity.

The result authorizes drafting a separate local, type-shared T4/T5 commissioning
preregistration. It does not authorize that training, candidate retention, output routing,
hover, gate flight or promotion. `report-summary.json` preserves the central measurements without
checking in model binaries or duplicating official CC BY 4.0 data.
